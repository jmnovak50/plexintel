services:
  immich-mcp:
    build: .
    image: immich-mcp:local
    restart: unless-stopped
    env_file: .env
    ports:
      - "127.0.0.1:8000:8000"
    read_only: true
    tmpfs:
      - /tmp:size=16m,noexec,nosuid
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

